import 'package:flutter/material.dart';

class CommunitySupport extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('Community Support Page'),
        ),
    );
  }
}